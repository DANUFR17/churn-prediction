Model artifacts are excluded from git (too large for version control).

Run 02-modeling-shap.ipynb to generate:
- xgb_churn_pipeline.pkl  (trained sklearn Pipeline)
- feature_names.pkl       (list of feature names after preprocessing)

Both files are required to run api.py and streamlit_app.py.